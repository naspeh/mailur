#!/bin/bash
# http://redsymbol.net/articles/unofficial-bash-strict-mode/
set -exuo pipefail
[ ! -f bin/env ] || . bin/env

env=${env:-"env"}

cd /opt/mailur
bin/install

pacman --noconfirm -S git

set +ux
. $env/bin/activate
set -ux

pip install -e .[test]
